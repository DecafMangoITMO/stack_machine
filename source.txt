section .data:
    hello: "Hello"
    pointer: hello
section .text:
    .loop:
        lit pointer
        push
        push
        lit 0
        cmp
        jz .end
        drop
        lit out
        pop
        lit pointer
        dup
        push
        inc
        over
        pop
        drop
        jmp .loop
    .end:
        halt
        

    